<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/primaryBackground</item>
        <item name="colorPrimaryDark">@color/secondaryGrey</item>
        <item name="colorAccent">@color/secondaryBlue</item>
        <item name="android:windowBackground">@color/primaryBackground</item>
        <item name="android:fontFamily">@font/proxima</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>